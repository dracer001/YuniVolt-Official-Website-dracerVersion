# Ignore the vendor directory
/vendor/

# Ignore Composer lock file if you don't want to track it
/composer.lock

# Ignore composer-specific files (if you don't want to track them)
composer.phar

# Optionally ignore cache directories (for Composer)
.cache/
